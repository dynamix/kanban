- method ||= :post
- item = @item || Item.new
- form_for item, :url => url, :method => method do |f|
  %div
    %label Title
    %br
    = f.text_field :title
    .clear
  %div
    %label Text
    %br
    = f.text_area :text
  %div.owner
    %label Owner
    %br
    = f.select :owner_id, User.all.collect{|u| [u.name, u.id]}
  %div.estimation
    %label Estimation
    %br
    = f.text_field :estimation
  %div.save
    = submit_tag 'Save', :id => "item-form-submit-#{method}"

  %p
    %label Last change by
    %br
    - if item.last_editor
      = item.last_editor.to_s
      - if item and (item.created_at or item.updated_at)
        ,
        = distance_of_time_in_words_to_now(item.updated_at || item.created_at)
        ago.
      = link_to "show history", item_versions_path(@project,@lane,item), :target => "_blank"  if item and item.version and item.version > 1
    - else
      NEVER EDITED!
