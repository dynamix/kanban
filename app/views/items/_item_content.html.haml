= hidden_field :item, :url, :value => edit_item_url(@project, item.lane, item)
%h3{klass(item.todo_state)}= item.title
- if item.owner
  %span.owner= item.owner
%span.wip= wip_for_item(item)
.tip.hidden
  = item.text.to_html
%br